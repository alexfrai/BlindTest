const assert = require('assert') ;

it('add test', (done) => {
  assert.ok(true);
  done();
});
/*
let calc = new Calc();

describe('tests mathematiques', () => {
  it('add test', (done) => {
    console.log(calc.addition(1,2));
    assert.ok(true);
    done();
  });

  it('sub test', (done) => {
    console.log(calc.substraction(1,5,1));
      assert.ok(true);
      done();
  });

  it('mult test', (done) => {
    console.log(calc.multiplication(2,4));
    assert.ok(true);
    done();
});

 it(`Test de ma fonction division par 0 arg 2`, (done) => {    
    assert.throws(() => calcullette.division(180, 0), Error);
    done();
 })

  it('add guess test', (done) => {
    //console.log(calc.addition(2,1));
    assert.equal(calc.addition(5,1), 6);
    done();
  });
}); 
*/